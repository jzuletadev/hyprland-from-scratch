# ~/hyprland-from-scratch/dotfiles/hypr/hyprland.conf
#
# Minimal Hyprland config — Phase 1 baseline.
# Every section is intentionally small so each future addition is a
# deliberate, understood change instead of inherited template noise.

# ---- Monitor ----
# ,preferred,auto,1  ->  any monitor, preferred resolution/refresh, auto position, scale 1
monitor = ,preferred,auto,1

# ---- Environment ----
env = XCURSOR_SIZE,24

# ---- Input ----
input {
    kb_layout = us
    follow_mouse = 1
    touchpad {
        natural_scroll = true
    }
}

# ---- General look (kept minimal on purpose) ----
general {
    gaps_in = 4
    gaps_out = 8
    border_size = 2
    col.active_border = rgba(88c0d0ff)
    col.inactive_border = rgba(3b4252aa)
    layout = dwindle
}

decoration {
    rounding = 6
}

# ---- Keybinds ----
$mainMod = SUPER
$terminal = kitty

bind = $mainMod, T, exec, $terminal
bind = $mainMod, Q, killactive,
bind = $mainMod, M, exit,
bind = $mainMod, V, togglefloating,

# move focus between windows
bind = $mainMod, left,  movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up,    movefocus, u
bind = $mainMod, down,  movefocus, d

# switch workspaces 1-5
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
# move active window to workspace 1-5
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5

exec-once = waybar
exec-once = dunst
exec-once = hypridle
bind = $mainMod, L, exec, hyprlock
bind = $mainMod, R, exec, rofi -show drun

bindel = ,XF86MonBrightnessUp, exec, brightnessctl set 5%+
bindel = ,XF86MonBrightnessDown, exec, brightnessctl set 5%-

# ~/hyprland-from-scratch/dotfiles/hypr/hyprlock.conf
#
# Minimal hyprlock config — Phase 7 baseline.
# Password field + clock only. Background image and theming com
